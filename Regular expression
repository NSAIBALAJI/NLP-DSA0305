import re
text = "Hello, this is a sample text with some email addresses: john@example.com and jane@example.co.uk."
pattern = r"\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b"
matches = re.findall(pattern, text)
for match in matches:
    print(match)
